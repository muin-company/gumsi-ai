# 검시AI - Next.js 프로젝트 셋업 완료

## 📦 설치된 패키지

### 핵심 프레임워크
- Next.js 14.2.35
- React 18.2.0
- TypeScript

### 스타일링
- Tailwind CSS
- PostCSS
- Autoprefixer

### 개발 도구
- ESLint (next/core-web-vitals)
- Prettier

## 📁 폴더 구조

```
gumsi-ai/
├── app/
│   ├── (auth)/
│   │   ├── login/          # 로그인 페이지
│   │   │   └── page.tsx
│   │   └── signup/         # 회원가입 페이지
│   │       └── page.tsx
│   ├── (main)/
│   │   ├── dashboard/      # 대시보드 (미구현)
│   │   ├── tutor/          # AI 튜터 (미구현)
│   │   ├── questions/      # 문제 풀이 (미구현)
│   │   └── profile/        # 프로필 (미구현)
│   ├── api/
│   │   ├── auth/           # 인증 API (미구현)
│   │   ├── chat/           # 채팅 API (미구현)
│   │   └── questions/      # 문제 API (미구현)
│   ├── layout.tsx          # 루트 레이아웃
│   ├── page.tsx            # 랜딩 페이지 ✅
│   └── globals.css         # 글로벌 스타일
├── components/
│   ├── ui/
│   │   ├── Button.tsx      # 버튼 컴포넌트 ✅
│   │   ├── Input.tsx       # 인풋 컴포넌트 ✅
│   │   ├── Card.tsx        # 카드 컴포넌트 ✅
│   │   └── index.ts
│   └── layout/
│       ├── Header.tsx      # 헤더 컴포넌트 ✅
│       ├── Footer.tsx      # 푸터 컴포넌트 ✅
│       └── index.ts
├── public/                 # 정적 파일
├── .env.example           # 환경변수 템플릿
├── .gitignore
├── next.config.js
├── tailwind.config.ts
├── tsconfig.json
├── .eslintrc.json
└── .prettierrc
```

## 🚀 실행 방법

### 1. 환경변수 설정
```bash
cp .env.example .env
# .env 파일을 열어 실제 값으로 수정
```

### 2. 개발 서버 실행
```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속

### 3. 빌드 및 프로덕션 실행
```bash
npm run build
npm start
```

## 📄 사용 가능한 스크립트

- `npm run dev` - 개발 서버 실행 (localhost:3000)
- `npm run build` - 프로덕션 빌드
- `npm start` - 프로덕션 서버 실행
- `npm run lint` - ESLint 검사
- `npm run format` - Prettier 포맷팅

## ✅ 구현 완료 페이지

1. **랜딩 페이지** (`/`)
   - Hero 섹션
   - 주요 기능 소개
   - CTA (Call to Action)

2. **로그인 페이지** (`/login`)
   - 이메일/비밀번호 폼
   - 비밀번호 찾기 링크
   - 회원가입 링크

3. **회원가입 페이지** (`/signup`)
   - 이름, 이메일, 비밀번호 폼
   - 비밀번호 확인
   - 이용약관 안내

## 🎨 UI 컴포넌트

### Button
```tsx
import { Button } from "@/components/ui/Button";

<Button variant="primary" size="md">클릭</Button>
<Button variant="outline">아웃라인</Button>
```

### Input
```tsx
import { Input } from "@/components/ui/Input";

<Input label="이메일" type="email" placeholder="your@email.com" />
```

### Card
```tsx
import { Card } from "@/components/ui/Card";

<Card hover>
  <h3>제목</h3>
  <p>내용</p>
</Card>
```

## 🔧 다음 단계

1. **Supabase 연동**
   - 인증 시스템 구현
   - 데이터베이스 스키마 설정

2. **AI 튜터 기능**
   - Anthropic Claude API 연동
   - 실시간 채팅 인터페이스

3. **문제 풀이 시스템**
   - 문제 데이터베이스 구축
   - 풀이 기록 및 분석

4. **대시보드**
   - 학습 통계
   - 진도 추적

## 📝 참고사항

- Next.js App Router 사용
- TypeScript strict 모드 활성화
- Tailwind CSS 유틸리티 클래스 사용
- 모든 페이지에 Header/Footer 레이아웃 필요 시 추가 가능

## 🐛 알려진 이슈

- `npm audit` 경고 1개 (high severity) - 프로덕션 배포 전 확인 필요
